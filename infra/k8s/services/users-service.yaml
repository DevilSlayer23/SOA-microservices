apiVersion: v1
kind: Service
metadata:
  name: users
  namespace: ecommerce
  labels:
    app: users
spec:
  type: NodePort
  selector:
    app: users
  ports:
  - port: 8001
    targetPort: 8001
    nodePort: 30001
    protocol: TCP