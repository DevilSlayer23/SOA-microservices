apiVersion: v1
kind: Service
metadata:
  name: products-service
  namespace: ecommerce
  labels:
    app: products-service
spec:
  type: NodePort
  selector:
    app: products-service
  ports:
  - port: 8002
    targetPort: 8002
    nodePort: 30002
    protocol: TCP